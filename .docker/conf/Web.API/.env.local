APP_ENV=dev
# API
JWT_SECRET=ChangeMe
API_PORT=3000
# Mongo
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=password
MONGO_INITDB_DATABASE=esgi-social-network
MONGO_CON_STRING=mongodb://host.docker.internal:27017/${MONGO_INITDB_DATABASE}
# Postgres
POSTGRES_USER=root
POSTGRES_PASSWORD=password
POSTGRES_DB=esgi-social-network
POSTGRES_CON_STRING=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@host.docker.internal:5432/${POSTGRES_DB}